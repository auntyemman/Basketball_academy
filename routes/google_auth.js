/*
const express = require('express');
const googleApi = express.Router();
const googleAuth = require('../middlewares/google_auth')
require('../src/config/google')

googleApi.get('/google', googleAuth, (req, res) =>{
    res.send(200);
});
googleApi.get('google/redirect', googleAuth, (req, res) => {
    res.send(200);
});

module.exports = googleApi;
*/